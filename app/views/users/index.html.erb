<% title "All users" %>
<h1>All users</h1>

<%= will_paginate @users %>

<ul class="user_index">
	
	<% @users.each do |user| %>
		<%= render 'user', :user => user %>
	<% end %>
	
</ul>

<%= will_paginate @users %>